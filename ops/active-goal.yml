schema_version: "1.0"
goal_id: IOT-T062
status: COMPLETE
objective: >-
  将已完成且本地验证通过的 IOT-T061 分支
  codex/iot-t061-workflow-runtime-contract 推送到 origin，并验证远端分支
  精确指向本地最终 HEAD。本目标只负责交付，不开 PR、不 merge、不 deploy，
  不修改 IOT-T061 的实现、内容或 trust 数据。
scope:
  task_id: IOT-T062
  mode: PROGRESSION_CONTRACT_HARDENING
  first_batch_articles: 0
  max_articles: 0
activated_by: "current-user-prompt-2026-07-16-authorize-push-and-handoff"
review_after: "2026-07-17"
budget:
  max_batches: 1
  max_articles: 0
  max_commits: 2
  max_wall_minutes: 60
  max_parallel_agents: 1
  max_parallel_writers: 1
external_outcome:
  definition: >-
    origin/codex/iot-t061-workflow-runtime-contract 存在，并在最终 push 后与
    本地 HEAD 完全一致；远端 main、PR、merge 和 Pages 部署状态均不改变。
  d_axis: D1
  satisfies_when:
    - 远端分支 refs/heads/codex/iot-t061-workflow-runtime-contract 存在。
    - git ls-remote 返回的远端分支 SHA 与本地最终 HEAD 完全一致。
    - 本地分支仍以 90de00ae421b29fa3e7231a5958e9a594fe328b3 为祖先，且不使用 force push。
    - IOT-T061 的 workflow policy、测试、文档和实现提交原样保留。
    - push 后工作树干净，active goal 为 IOT-T062 COMPLETE。
    - 不创建 PR、不 merge、不 deploy，也不修改远端 main。
external_action_authority:
  commit: true
  push: true
  open_pr: false
  merge: false
  deploy: false
external_action_grant:
  source: "current-user-prompt-2026-07-16-authorize-push-and-handoff"
  granted_at: "2026-07-16T02:07:28Z"
  scope: >-
    仅限 IOT-T062：允许为 push-only 交付合同和 closeout 创建不超过 2 个
    原子本地提交，并把当前 codex/iot-t061-workflow-runtime-contract 分支
    普通 push 到 origin。禁止 force push、开 PR、merge、deploy、修改 main，
    或修改 ops/active-goal.yml 与 lock 之外的任何文件。
stop_conditions:
  - 工作树出现 ops/active-goal.yml 与 ops/active-goal.lock.json 之外的改动。
  - 远端同名分支已存在且不是本地 HEAD 的祖先，普通 push 会产生非快进。
  - origin/main 不再以 90de00ae421b29fa3e7231a5958e9a594fe328b3 为当前远端 main，导致交付基线需要重审。
  - goal/lock 不一致，或 Python 3.11 active-goal 校验环境不可复现。
  - push 需要 force、删除、PR、merge、deploy、凭证落盘或扩大授权才能继续。
  - push 后远端分支 SHA 无法与本地 HEAD 对齐。
  - 预算耗尽或 review_after 到达 2026-07-17。
superseded_by: null
baseline:
  prepared_at_ref: c9c3ce4db58af8bf57b31d2062da2eb5b50da8d2
  remote_main_ref: 90de00ae421b29fa3e7231a5958e9a594fe328b3
  required_history_ancestor: 90de00ae421b29fa3e7231a5958e9a594fe328b3
  expected_inventory:
    canonical_content: 697
    plan_topics: 1761
  expected_trust:
    source_records: 1287
    review_records: 1284
    legacy_unbound: 0
    evidence_bound_review: 642
    verified: 642
    approved: 642
acceptance_checks:
  - id: active_goal_contract
    cadence: EVERY_SLICE
    command: .tmp/agent-venv/bin/python tools/check_active_goal.py
    expectation: ACTIVE_GOAL_OK goal_id=IOT-T062 during delivery and status=COMPLETE at closeout.
  - id: workflow_policy
    cadence: EVERY_SLICE
    command: .tmp/agent-venv/bin/python tools/check_workflow_policy.py
    expectation: the delivered IOT-T061 workflow runtime and dependency policy remains valid.
  - id: required_ancestor
    cadence: EVERY_SLICE
    command: git merge-base --is-ancestor 90de00ae421b29fa3e7231a5958e9a594fe328b3 HEAD
    expectation: the delivery branch remains a descendant of the refreshed remote main baseline.
  - id: remote_branch_exact
    cadence: GOAL_CLOSE
    command: test "$(git ls-remote --heads origin refs/heads/codex/iot-t061-workflow-runtime-contract | awk '{print $1}')" = "$(git rev-parse HEAD)"
    expectation: the remote branch SHA exactly equals the final local HEAD.
  - id: clean_worktree
    cadence: GOAL_CLOSE
    command: test -z "$(git status --porcelain)"
    expectation: no tracked or untracked worktree changes remain.
  - id: diff_check
    cadence: EVERY_SLICE
    command: git diff --check
    expectation: no whitespace errors.
allowed_mutations:
  - ops/active-goal.yml
  - ops/active-goal.lock.json
forbidden_mutations:
  - AGENTS.md
  - .github/
  - CHANGELOG.md
  - VERSION
  - schemas/
  - tools/
  - tests/
  - docs/
  - data/
  - papers/
  - plans/
selection:
  policy: >-
    IOT-T062 是非文章、push-only 交付目标；不选择文章，不修改实现或可信状态。
  selected_articles: []
  rationale: "Push-only delivery of the completed IOT-T061 branch."
progress:
  completed_batches: 1
  completed_articles: 0
  completed_commits: 2
  no_external_delta_batches: 0
  last_result: IOT_T062_REMOTE_BRANCH_FINAL_HEAD_VERIFIED
  last_external_delta: REMOTE_BRANCH_PUSHED_VIA_EXISTING_SSH_IDENTITY
activation:
  ref: c9c3ce4db58af8bf57b31d2062da2eb5b50da8d2
  activated_at: "2026-07-16T02:07:28Z"
next_action:
  kind: NONE
  requires_activation: false
  instruction: >-
    IOT-T062 is complete after the final closeout push. Do not open a PR, merge,
    deploy, or start another IoT goal without a new explicit user request.
